export const colors = {
  // Brand Saffron / Warm Orange (Extracted directly from buttons & accents)
  primary: "#F3AB52",          // Main Saffron ("Call & Book", "Voice Search", "Post")
  primaryDark: "#DC8E2D",      // Deep Ochre (Hover, active button states)
  primaryLight: "#FDE2B5",     // Soft Light Orange ("Match Reasons" tag container)
  primarySoft: "#FFF7ED",      // Warm subtle tint (Highlighted panels)

  // Background & Surfaces
  background: "#F6EFE6",       // Warm Ivory / Cream (Outer canvas backdrop)
  surface: "#FFFFFF",          // Pure White (Worker cards, modals, input fields)
  surfaceSecondary: "#ECE4D8",  // Muted Cream / Grey (Inactive role toggles)

  // High-Contrast Typography
  text: "#1E1E1E",             // Primary Black (Worker names, titles, input text)
  textSecondary: "#666666",    // Medium Neutral Grey (Categories, sub-labels, distance)
  textMuted: "#8C8C8C",        // Muted Grey (Search placeholder, secondary details)

  // Borders & Outlines
  border: "#E0D7CC",           // Light Warm Grey (Card outlines & input borders)
  borderStrong: "#333333",     // Dark Outline (Search bar container & primary inputs)

  // Feedback & State Colors
  success: "#2E7D32",          // Clean Green (Available / Verified indicators)
  warning: "#E65100",          // Amber / Orange (Urgent bookings)
  error: "#D32F2F",            // Red (Form errors / Disconnected)

  // Neutrals
  white: "#FFFFFF",
  black: "#000000",
};